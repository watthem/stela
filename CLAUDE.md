@AGENTS.md

# Claude Code

- Use the shared private companion workspace's `agent/WORK.md` and `MEMORY.md`; do not create a second tracker in Claude's host-local memory.
- Start cross-repository sessions with the private companion workspace as an explicit `--add-dir`.
- Keep Claude-specific operational notes here; shared project rules belong in `AGENTS.md`.
